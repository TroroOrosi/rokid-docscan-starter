fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
pillow>=10.0
pytest>=8.0
httpx>=0.27

# Optional — only needed to enable the real cloud model adapters
# (ROKID_ANALYZER/SOLVER/EXPLAINER/EXTRACTOR=claude + ANTHROPIC_API_KEY).
# The server runs fully offline on the local placeholders without it.
#   pip install anthropic
# anthropic>=0.40
