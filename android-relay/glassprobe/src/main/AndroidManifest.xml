<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- The probe has to install and report even where a capability is absent;
         an install refusal would be indistinguishable from a negative result. -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.any" android:required="false" />

    <application
        android:allowBackup="false"
        android:label="DocScan Capability Probe"
        android:supportsRtl="false"
        android:usesCleartextTraffic="true">

        <!-- No android:screenOrientation. Declaring "landscape" rotates the
             glasses display 90 degrees; the system default is correct here. -->
        <activity
            android:name=".CapabilityProbeActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@android:style/Theme.Material.NoActionBar.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
